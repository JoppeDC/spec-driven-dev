---
name: run-tests
description: Run the PHPUnit test suite. Use this when asked to run tests, verify the application works, or check if generated code is correct.
---

Run the full PHPUnit test suite using:

```bash
php vendor/bin/phpunit
```

Report the total number of tests, assertions, and any failures or errors.
If tests fail, include the failure messages so they can be acted on.
